// SendGrid integration stub — in production, emails are sent via the Flask backend
// This file exists for reference and potential future client-side needs

export const SENDGRID_NOTE = 'Email sending is handled by the Flask backend via POST /api/send-campaign'
